import { createWebHistory , createRouter } from "vue-router";
//const CoachDetail = () => import('./pages/Home.vue')
const router = createRouter({
  history:createWebHistory(),
  routes : [
  //{path:'/' , component:Home}
  ],
})

export default router